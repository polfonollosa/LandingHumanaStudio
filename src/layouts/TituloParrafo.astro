---
// Definimos las propiedades (props) que recibirá el componente
const { title,name, description } = Astro.props;
---

<div class="w-5/6 h-3/4 flex-col flex justify-center space-y-6">
    <div class="w-full space-y-3">
      <p class="px-24  text-lg text-black font-semibold">"{description}"</p>
      <div class="flex flex-col justify-center px-24 text-lg">
        <h3 class="text-black font-semibold">{name}</h3>
        <h3 class="text-gray-500 font-semibold">{title}</h3>
      </div>
    </div>
</div>